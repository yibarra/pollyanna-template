import React from 'react';
import PropTypes from 'prop-types';

import './events.scss';

const Events = () => {
  // return
  return (
    <div className="page bio">
      <h1>Events</h1>
    </div>
  )
};

Events.propTypes = {
  any: PropTypes.any,
}

export default Events;
