import React from 'react';
import PropTypes from 'prop-types';

import './bio.scss';

const Bio = () => {
  // return
  return (
    <div className="page bio">
      <h1>Bio</h1>
    </div>
  )
};

Bio.propTypes = {
  any: PropTypes.any,
}

export default Bio;