import React from 'react';
import PropTypes from 'prop-types';

import './home.scss';

const Home = () => {
  // return
  return (
    <div className="page home">
    </div>
  );
};

Home.propTypes = {
  any: PropTypes.any,
};

export default Home;