import React from 'react';
import PropTypes from 'prop-types';

import './contact.scss';

// contact
const Contact = props => {
  return (
    <div className="contact">

    </div>
  );
};

Contact.propTypes = {
  any: PropTypes.any,
};

export default Contact;