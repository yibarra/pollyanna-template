import React from 'react';
import PropTypes from 'prop-types';

import './footer.scss';

// Footer
const Footer = props => {
  // render
  return (
    <footer>
      footer
    </footer>
  )
};

Footer.propTypes = {
  any: PropTypes.any,
};

export default Footer;