import React from 'react';
import PropTypes from 'prop-types';

import './header-events.scss';

const HeaderEvents = () => {
  return (
    <div className="header--events">
      
    </div>
  );
};

HeaderEvents.propTypes = {
  any: PropTypes.any,
};

export default HeaderEvents;