import React from 'react';
import PropTypes from 'prop-types';

import './header.scss';

// Header
const Header = props => {
  // render
  return (
    <header>
      header
    </header>
  )
};

Headers.propTypes = {
  any: PropTypes.any,
};

export default Header;